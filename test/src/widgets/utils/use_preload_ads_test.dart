import 'package:flutter_test/flutter_test.dart';
import 'package:kontext_flutter_sdk/src/widgets/utils/use_preload_ads.dart';

void main() {
  testWidgets('runs usePreloadAds without crashing', (tester) async {

    expect(true, isTrue);
  });
}
