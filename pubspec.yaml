name: kontext_flutter_sdk
description: Kontext Flutter SDK
version: 0.0.1
homepage: https://www.kontext.so/

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
